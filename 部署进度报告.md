# 云原生微服务电商系统 - 部署进度报告

**生成时间**: 2026-07-10  
**服务器**: 阿里云ECS (iZ7xv55z9wkeldvtauj3kzZ)  
**公网IP**: 8.163.25.118

---

## 📊 整体进度: 50%

### ✅ 已完成的工作 (50%)

#### 1. 服务器环境准备
- [x] 阿里云ECS Ubuntu 22.04（4核8G）已就绪
- [x] Docker已安装并运行
- [x] Java 17 + Maven 3.6.3已安装
- [x] SSH密码登录可用（密钥登录配置失败，阿里云ECS限制）
- [x] Docker镜像源配置完成（daocloud）

#### 2. Kubernetes集群搭建
- [x] Minikube v1.32.0已安装
- [x] K8s v1.28.3集群运行正常
- [x] 使用Docker驱动 + `--force`参数
- [x] K8s核心组件镜像已加载：
  - kube-apiserver:v1.28.3 ✅
  - kube-controller-manager:v1.28.3 ✅
  - kube-scheduler:v1.28.3 ✅
  - kube-proxy:v1.28.3 ✅
  - etcd:3.5.9-0 ✅
  - coredns:v1.10.1 ✅
  - pause:3.9 ✅
  - storage-provisioner:v5 ✅

#### 3. 项目代码准备
- [x] ecommerce-project文件夹已上传到 `/root/`
- [x] 包含cart-service、user-service、product-service
- [x] k8s配置文件已就绪

#### 4. 购物车服务编译构建
- [x] Maven编译成功：`BUILD SUCCESS`
- [x] Docker镜像构建成功：`cart-service:v0`（191MB压缩后）
- [x] 镜像已成功加载到**Minikube内部Docker**
- [x] 验证通过：`minikube image ls | grep cart` → `docker.io/library/cart-service:v0`

---

### ❌ 未完成的工作 (50%)

#### 1. 中间件镜像问题（当前卡点）
**问题描述**: 所有外部Docker镜像源在当前网络环境下均不可用

| 镜像 | 宿主机Docker | Minikube内部 | 状态 |
|------|-------------|-------------|------|
| cart-service:v0 | ✅ 有 | ✅ 有 | **已完成** |
| redis:7 | ✅ 有 | ✅ 有 | **已完成** ⭐ |
| mysql:8.0 | ✅ 有 | ✅ 有 | **已完成** ⭐ |
| mongo:6.0 | ❌ 无 | ❌ 无 | **待解决**（tar文件格式错误） |

**失败原因**:
- ✅ cart-service镜像已通过 `docker cp` + `docker exec load` 成功加载
- ❌ Redis镜像拉取测试已开始但超时（daocloud网络不稳定）
- ❌ 本地Windows没有Docker Desktop，无法下载镜像上传

**已尝试的解决方案**:
1. ✅ 配置Docker镜像源为 `https://docker.m.daocloud.io`
2. ✅ 使用 `docker save` + `docker cp` + `docker exec load` 成功加载cart-service
3. ✅ **在本地Windows下载redis:7.tar并上传到服务器，成功加载到Minikube** ⭐
4. ✅ **MySQL 8.0镜像已成功加载并部署** ⭐
5. ❌ MongoDB 6.0 tar文件格式错误，需要重新下载
6. ❌ SSH密钥登录配置失败（阿里云ECS限制了SSH密钥认证）

**完整操作代码记录**:

```bash
# ========== 1. Docker配置 ==========
# 配置daocloud镜像源
cat > /etc/docker/daemon.json << 'EOF'
{
  "builder": {
    "gc": {
      "defaultKeepStorage": "20GB",
      "enabled": true
    }
  },
  "experimental": false,
  "registry-mirrors": [
    "https://docker.m.daocloud.io"
  ]
}
EOF

systemctl daemon-reload
systemctl restart docker

# 验证配置
docker info | grep -A 3 "Registry Mirrors"

# ========== 2. cart-service镜像加载（成功）==========
# 导出镜像
docker save cart-service:v0 -o /tmp/cart-service.tar

# 复制到Minikube容器
docker cp /tmp/cart-service.tar ca2361b19cec:/var/lib/docker/tmp/cart-service.tar

# 在Minikube容器内加载
docker exec ca2361b19cec docker load -i /var/lib/docker/tmp/cart-service.tar

# 验证
minikube image ls | grep cart
# 输出: docker.io/library/cart-service:v0

# ========== 3. Redis镜像加载（成功）==========
# 在本地Windows下载Redis镜像
docker pull redis:7
docker save redis:7 -o D:\redis7.tar

# 上传到服务器
scp D:\redis7.tar root@8.163.25.118:/root/

# 在服务器加载到宿主机Docker
docker load -i /root/redis7.tar

# 复制到Minikube容器并加载
CONTAINER_ID=$(docker ps -q -f name=minikube)
docker cp /root/redis7.tar $CONTAINER_ID:/var/lib/docker/tmp/redis7.tar
docker exec $CONTAINER_ID docker load -i /var/lib/docker/tmp/redis7.tar

# 验证
minikube image ls | grep redis
# 输出: docker.io/library/redis:7

# 重新部署Redis
kubectl delete pod -l app=redis
kubectl get pods
# 输出: redis-7b568468dc-x9cnl   1/1     Running

# ========== 4. MySQL镜像加载（成功）==========
# 在本地Windows下载MySQL镜像
docker pull mysql:8.0
docker save mysql:8.0 -o D:\mysql8.tar

# 上传到服务器
scp D:\mysql8.tar root@8.163.25.118:/root/

# 在服务器加载到宿主机Docker
docker load -i /root/mysql8.tar

# 复制到Minikube容器并加载
CONTAINER_ID=$(docker ps -q -f name=minikube)
docker cp /root/mysql8.tar $CONTAINER_ID:/var/lib/docker/tmp/mysql8.tar
docker exec $CONTAINER_ID docker load -i /var/lib/docker/tmp/mysql8.tar

# 验证
minikube image ls | grep mysql
# 输出: docker.io/library/mysql:8.0

# 部署MySQL
kubectl apply -f /root/ecommerce-project/k8s/mysql-deployment.yaml
kubectl get pods
# 输出: mysql-774b876447-896ts   1/1     Running

# ========== 5. MongoDB镜像问题 ==========
# mongo6.tar文件格式错误，无法加载
# 需要重新从本地Windows下载正确的MongoDB镜像
docker pull mongo:6.0
docker save mongo:6.0 -o D:\mongo6.tar
# 然后重新上传到服务器
# 生成本地公钥
ssh-keygen -y -f C:\Users\86187\.ssh\id_rsa > C:\Users\86187\.ssh\id_rsa.pub

# 添加公钥到服务器
ssh root@8.163.25.118 "echo '<公钥内容>' >> ~/.ssh/authorized_keys"

# 修复权限
ssh root@8.163.25.118 "chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys"

# 启用PubkeyAuthentication
ssh root@8.163.25.118 "sed -i 's/^#PubkeyAuthentication yes/PubkeyAuthentication yes/' /etc/ssh/sshd_config"
ssh root@8.163.25.118 "systemctl restart sshd"

# 测试免密登录（仍需密码）
ssh -i C:\Users\86187\.ssh\id_rsa root@8.163.25.118 "echo test"
# 结果: 仍需输入密码，阿里云ECS可能禁用了SSH密钥认证
```

#### 2. 中间件部署（大部分完成）
- [x] Redis部署（redis:7镜像已成功加载，Pod运行正常）⭐
- [x] MySQL部署（mysql:8.0镜像已成功加载，Pod运行正常）⭐
- [ ] MongoDB部署（mongo6.tar格式错误，需重新下载）

#### 3. 微服务部署（准备中）
- [x] 购物车服务镜像已加载到Minikube
- [ ] 购物车服务部署（依赖Redis和MySQL，均已就绪）
- [ ] 用户服务部署
- [ ] 商品服务部署
- [ ] Web网关部署

#### 4. 测试验证（待完成）
- [ ] 端口转发配置
- [ ] 功能测试
- [ ] 数据库验证

---

## 🎯 下一步操作方案

### 方案A：在本地Windows下载中间件镜像后上传（已成功用于Redis）⭐

**优势**: 不依赖服务器网络，直接提供镜像文件
**成果**: ✅ Redis镜像已通过此方法成功部署

**步骤**:
```bash
# 1. 在本地Windows PowerShell执行（需要Docker Desktop）
docker pull redis:7
docker save redis:7 -o D:\redis7.tar

# 2. 上传到服务器
scp D:\redis7.tar root@8.163.25.118:/root/

# 3. 在服务器VNC终端执行
docker load -i /root/redis7.tar
docker cp /root/redis7.tar <minikube-container-id>:/var/lib/docker/tmp/redis7.tar
docker exec <minikube-container-id> docker load -i /var/lib/docker/tmp/redis7.tar

# 4. 验证
minikube image ls | grep redis

# 5. 重新部署Redis
kubectl delete pod -l app=redis
kubectl get pods -w
```

**预期结果**: 
- Redis镜像成功加载到Minikube
- Redis Pod可以正常启动
- 可以继续部署其他微服务

---

### 方案B：跳过中间件，先部署cart-service测试

**优势**: 可以先验证K8s部署流程是否正常  
**劣势**: cart-service可能因缺少Redis而启动失败

```bash
# 1. 修改cart-deployment.yaml，移除Redis依赖或使用内存存储
# 2. 部署cart-service
kubectl apply -f /root/ecommerce-project/k8s/cart-deployment.yaml
kubectl get pods -w
```

---

### 方案C：改用kubeadm重新部署（长期方案）

**优势**: 符合《阿里云部署方案.md》，更适合生产环境  
**劣势**: 需要删除当前Minikube，重新搭建，且仍然面临镜像问题

**步骤**:
1. 删除Minikube集群
2. 安装kubeadm、kubelet、kubectl
3. 初始化K8s集群（使用腾讯云镜像源）
4. 部署Flannel网络插件
5. 部署中间件和应用（仍需解决镜像问题）

---

## 📝 关键命令记录

### 集群管理
```bash
# 查看节点状态
kubectl get nodes

# 查看所有Pod
kubectl get pods -A

# 查看Minikube状态
minikube status

# 删除集群
minikube delete
```

### 镜像管理
```bash
# 查看宿主机Docker镜像
docker images

# 查看Minikube内部镜像
minikube image ls

# 导出镜像
docker save <image>:<tag> -o /tmp/<image>.tar

# 加载镜像到Minikube
minikube ssh -- "docker load -i /tmp/<image>.tar"
```

### 部署管理
```bash
# 部署应用
kubectl apply -f <yaml-file>

# 查看Pod状态
kubectl get pods

# 查看Pod详细信息
kubectl describe pod <pod-name>

# 查看日志
kubectl logs -f <pod-name>

# 删除部署
kubectl delete -f <yaml-file>
```

---

## ⚠️ 已知问题和解决方案

### 问题1: SSH密码认证失败
**现象**: `Permission denied, please try again`  
**原因**: SSH配置被修改，PasswordAuthentication可能被设为no  
**解决**: 通过VNC登录，修改 `/etc/ssh/sshd_config`，确保：
```
PasswordAuthentication yes
PermitRootLogin yes
```
然后重启SSH: `systemctl restart sshd`

### 问题2: 镜像源超时
**现象**: `TLS handshake timeout` 或 `403 Forbidden`  
**原因**: 中国大陆网络环境限制  
**解决**: 使用 `docker save/load` 方式绕过网络拉取

### 问题3: minikube image load卡住
**现象**: 命令执行后一直等待，无输出  
**原因**: Minikube内部Docker与宿主机通信问题  
**解决**: 使用以下方法替代：
```bash
docker save <image>:<tag> -o /tmp/<image>.tar
docker cp /tmp/<image>.tar <minikube-container-id>:/var/lib/docker/tmp/<image>.tar
docker exec <minikube-container-id> docker load -i /var/lib/docker/tmp/<image>.tar
```

### 问题4: 所有外部Docker镜像源不可用
**现象**: `Client.Timeout exceeded` 或 `TLS handshake timeout`  
**原因**: 中国大陆网络环境限制，所有镜像源（daocloud、1ms.run、阿里云、腾讯云）均不可用  
**解决**: 
1. 在本地Windows下载镜像并保存为tar文件
2. 上传到服务器
3. 使用上述方法加载到Minikube
4. 或者暂时跳过中间件部署，先测试应用服务

---

## 📞 联系方式

如有问题，请查阅：
- 《搭建指导手册.md》- 本地WSL部署指南
- 《阿里云部署方案.md》- 云端生产部署指南

---

**最后更新**: 2026-07-10 23:00  
**当前状态**: ✅ Redis和MySQL已成功部署并运行，MongoDB需重新下载  
**下次计划**: 
1. 在本地Windows重新下载mongo:6.0镜像（确保格式正确）
2. 上传到服务器并加载到Minikube
3. 部署MongoDB
4. 部署微服务应用（cart-service、user-service等）
