# 阿里云ECS快速部署指南 - 云原生微服务电商系统

## 📌 前置条件

> **重要**: 在执行部署前，请确保后端和前端开发人员已提供以下文件：
>
> **后端需提供：**
> - `cart-service/Dockerfile`、`user-service/Dockerfile`、`product-service/Dockerfile`、`order-service/Dockerfile`
> - `k8s/cart-deployment.yaml`、`k8s/user-deployment.yaml`、`k8s/product-deployment.yaml`、`k8s/order-deployment.yaml`
> - MySQL建表脚本（orders和order_items表）
>
> **前端需提供：**
> - `frontend/` 目录（React + Vite项目）
> - `frontend/Dockerfile`（多阶段构建）
> - `frontend/nginx.conf`（API反向代理配置）
> - `k8s/frontend-deployment.yaml`

---

##  您的环境信息

| 项目 | 值 |
|------|-----|
| **实例ID** | iZ7xv55z9wkeldvtauj3kzZ |
| **公网IP** | 8.163.25.118 |
| **私网IP** | 172.31.89.122 |
| **SSH用户** | root |
| **SSH密码** | 123$%^&*()QAZwsxedc |
| **操作系统** | Ubuntu 22.04 64位 |
| **配置** | 4核 vCPU / 8GB RAM |
| **地域** | 华南3（广州）A |

---

## 🚀 快速开始（3步完成部署）

### 步骤1：连接到服务器

```bash
# Windows PowerShell 或 CMD（使用自定义端口22222）
ssh -p 22222 root@8.163.25.118
# 输入密码: 123$%^&*()QAZwsxedc
```

> ⚠️ **如果SSH连接失败**：请在阿里云控制台 → 网络与安全组中，将SSH(22222)规则的授权对象改为 `0.0.0.0/0` 或添加您的本地公网IP

> 💡 **推荐**：配置SSH密钥登录，避免每次输入密码。详见《阿里云部署方案.md》第2.2节

---

### 步骤2：上传项目文件到服务器

在本地Windows PowerShell中执行：

```powershell
# 进入项目目录
cd d:\Project\paral\CourseProject

# 上传整个ecommerce-project文件夹到服务器（使用自定义端口22222）
scp -P 22222 -r ecommerce-project root@8.163.25.118:/root/
```

> ⚠️ **前提条件**：确保后端和前端开发人员已将所有必需文件提交到项目中（见前置条件）

---

### 步骤3：连接到服务器并执行部署

SSH连接到服务器后，按顺序执行以下命令：

```bash
# 1. 系统准备
echo "[1/9] 更新系统..."
apt update && apt upgrade -y
apt install -y curl wget vim git htop net-tools docker.io containerd

# 2. 启动Docker
echo "[2/9] 启动Docker..."
systemctl start docker
systemctl enable docker

# 3. 安装K8s组件
echo "[3/9] 安装Kubernetes组件..."
apt install -y apt-transport-https ca-certificates
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.28/deb/Release.key | gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.28/deb/ /' | tee /etc/apt/sources.list.d/kubernetes.list
apt update && apt install -y kubelet kubeadm kubectl
apt-mark hold kubelet kubeadm kubectl
systemctl enable --now kubelet

# 4. 初始化集群
echo "[4/9] 初始化Kubernetes集群..."
swapoff -a
kubeadm init --pod-network-cidr=10.244.0.0/16 --apiserver-advertise-address=172.31.89.122 --cri-socket=unix:///var/run/containerd/containerd.sock
mkdir -p $HOME/.kube && cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
chown $(id -u):$(id -g) $HOME/.kube/config

# 5. 安装网络插件
echo "[5/9] 安装Flannel网络插件..."
kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml
sleep 30

# 6. 预加载镜像
echo "[6/9] 预加载基础镜像..."
docker pull redis:7-alpine
docker pull mysql:8.0
docker pull mongo:6.0

# 7. 进入项目目录
echo "[7/9] 进入项目目录..."
cd ~/ecommerce-project

# 8. 部署中间件和微服务
echo "[8/9] 部署应用..."
kubectl create namespace middleware --dry-run=client -o yaml | kubectl apply -f -
kubectl create namespace ecommerce --dry-run=client -o yaml | kubectl apply -f -

# 部署中间件
kubectl apply -f k8s/redis-deployment.yaml
kubectl apply -f k8s/mysql-deployment.yaml
kubectl apply -f k8s/mongodb-deployment.yaml

sleep 60

# 部署微服务（等待团队成员提供order-service和frontend的配置文件）
kubectl apply -f k8s/cart-deployment.yaml
kubectl apply -f k8s/user-deployment.yaml
kubectl apply -f k8s/product-deployment.yaml
# kubectl apply -f k8s/order-deployment.yaml  # 待后端提供
# kubectl apply -f k8s/frontend-deployment.yaml  # 待前端提供

# 9. 验证部署
echo "[9/9] 验证部署..."
kubectl get pods -A

echo "\n========================================"
echo "✅ 基础部署完成！"
echo "访问地址: http://8.163.25.118:30080"
echo "========================================"
echo "⚠️ 注意：order-service和frontend需要团队成员提供配置文件后才能部署"
```

部署完成后（约需10-15分钟），在浏览器访问：

```
http://8.163.25.118:30080
```

---

## 🔍 常用运维命令

### 查看所有服务状态

```bash
# 查看所有Pod（所有命名空间）
kubectl get pods -A

# 仅查看电商服务（包括新增的order-service和frontend）
kubectl get pods -n ecommerce

# 仅查看中间件（Redis/MySQL/MongoDB）
kubectl get pods -n middleware

# 查看服务列表
kubectl get svc -A

# 查看资源使用情况
kubectl top nodes
kubectl top pods -A
```

### 查看和跟踪日志

```bash
# 查看购物车服务最近100行日志
kubectl logs -n ecommerce -l app=cart-service --tail=100

# 查看订单服务日志（新增）
kubectl logs -n ecommerce -l app=order-service --tail=100

# 实时跟踪用户服务日志（Ctrl+C退出）
kubectl logs -n ecommerce -f -l app=user-service

# 查看特定Pod的日志
kubectl logs -n ecommerce <pod-name>

# 查看最近的错误日志
kubectl logs -n ecommerce -l app=cart-service | grep -i error
```

### 重启和更新服务

```bash
# 重启购物车服务
kubectl rollout restart deployment/cart-service -n ecommerce

# 重启订单服务（新增）
kubectl rollout restart deployment/order-service -n ecommerce

# 重启前端应用（新增）
kubectl rollout restart deployment/frontend -n ecommerce

# 重启所有电商服务
kubectl rollout restart deployment -n ecommerce

# 查看重启状态
kubectl rollout status deployment/cart-service -n ecommerce

# 回滚到上一个版本
kubectl rollout undo deployment/cart-service -n ecommerce
```

### 进入容器调试

```bash
# 获取Pod名称
kubectl get pods -n middleware -l app=redis

# 进入Redis容器
kubectl exec -it -n middleware $(kubectl get pods -n middleware -l app=redis -o jsonpath='{.items[0].metadata.name}') -- redis-cli

# 进入MySQL容器
kubectl exec -it -n middleware $(kubectl get pods -n middleware -l app=mysql -o jsonpath='{.items[0].metadata.name}') -- mysql -uroot -prootpassword

# 进入MongoDB容器
kubectl exec -it -n middleware $(kubectl get pods -n middleware -l app=mongodb -o jsonpath='{.items[0].metadata.name}') -- mongosh

# 进入任意容器的bash shell
kubectl exec -it -n ecommerce <pod-name> -- bash
```

### 测试API接口

```bash
# 在服务器上测试（需要先安装curl）
apt install -y curl

# 测试注册接口
curl -X POST http://localhost:30080/users/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456"}'

# 测试商品查询
curl http://localhost:30080/products/1

# 测试评价查询
curl http://localhost:30080/reviews/1

# 测试创建订单（新增）
curl -X POST http://localhost:30080/orders \
  -H "Content-Type: application/json" \
  -d '{"userId":1}'

# 测试查询用户订单（新增）
curl http://localhost:30080/orders/user/1

# 从本地电脑测试（替换localhost为服务器IP）
curl http://8.163.25.118:30080/products/1
```

### 实用运维技巧

```bash
# 导出当前配置备份
kubectl config view > k8s-config-backup.yaml

# 实时监控集群资源
watch -n 1 'kubectl top pods -A'

# 快速检查某个服务是否健康
kubectl get pods -n ecommerce -l app=cart-service -o wide

# 查看服务的事件（排查问题用）
kubectl get events -n ecommerce --sort-by='.lastTimestamp' | tail -20
```

---

## ⚠️ 常见问题排查

### 问题1：Pod一直处于Pending或ContainerCreating状态

```bash
# 检查原因
kubectl describe pod -n ecommerce <pod-name>

# 常见原因：
# 1. 镜像拉取失败 -> 检查镜像是否存在
docker images | grep service

# 2. 资源不足 -> 检查节点资源
kubectl top nodes

# 3. PVC绑定失败 -> 检查存储类
kubectl get pvc -A
```

### 问题2：服务间通信失败

```bash
# 检查Service是否存在
kubectl get svc -n middleware
kubectl get svc -n ecommerce

# 检查Endpoints
kubectl get ep -n middleware
kubectl get ep -n ecommerce

# 测试DNS解析
kubectl run test-dns --rm -it --image=busybox -- nslookup cart-service.ecommerce.svc.cluster.local
```

### 问题3：无法访问Web页面

```bash
# 检查Web服务是否运行
kubectl get pods -n ecommerce -l app=web-service

# 检查NodePort是否正确
kubectl get svc web-service -n ecommerce

# 检查安全组规则（在阿里云控制台确认30080端口已开放）

# 测试本地访问
curl http://localhost:30080
```

### 问题4：数据库连接失败

```bash
# 检查MySQL是否就绪
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=mysql -o jsonpath='{.items[0].metadata.name}') -- mysqladmin ping -uroot -prootpassword

# 检查MongoDB是否就绪
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=mongodb -o jsonpath='{.items[0].metadata.name}') -- mongosh --eval "db.adminCommand('ping')"

# 检查Redis是否就绪
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=redis -o jsonpath='{.items[0].metadata.name}') -- redis-cli ping
```

---

## 🔄 更新应用版本

当您修改了代码后，按以下步骤更新：

### 后端微服务更新（以cart-service为例）

```bash
# 1. 进入项目目录
cd ~/ecommerce-project/cart-service

# 2. 重新编译
mvn clean package -DskipTests

# 3. 构建新镜像（版本号递增）
docker build -t cart-service:v2 .

# 4. 推送到ACR（可选，如果配置了）
# docker tag cart-service:v2 registry.cn-hangzhou.aliyuncs.com/<命名空间>/cart-service:v2
# docker push registry.cn-hangzhou.aliyuncs.com/<命名空间>/cart-service:v2

# 5. 更新部署
kubectl set image deployment/cart-service cart-service=cart-service:v2 -n ecommerce

# 6. 查看更新状态
kubectl rollout status deployment/cart-service -n ecommerce

# 7. 验证新版本
kubectl get pods -n ecommerce -l app=cart-service
```

### 前端React SPA更新

```bash
# 1. 进入前端项目目录
cd ~/ecommerce-project/frontend

# 2. 安装依赖并构建
npm install
npm run build

# 3. 构建新镜像
docker build -t frontend:v2 .

# 4. 更新部署
kubectl set image deployment/frontend nginx=frontend:v2 -n ecommerce

# 5. 查看更新状态
kubectl rollout status deployment/frontend -n ecommerce

# 6. 验证
kubectl get pods -n ecommerce -l app=frontend
```

### 订单服务更新

```bash
# 1. 进入订单服务项目目录
cd ~/ecommerce-project/order-service

# 2. 重新编译
mvn clean package -DskipTests

# 3. 构建新镜像
docker build -t order-service:v2 .

# 4. 更新部署
kubectl set image deployment/order-service order-service=order-service:v2 -n ecommerce

# 5. 查看更新状态
kubectl rollout status deployment/order-service -n ecommerce
```

---

## 💾 数据备份

### MySQL备份

```bash
# 手动备份
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=mysql -o jsonpath='{.items[0].metadata.name}') -- mysqldump -uroot -prootpassword ecommerce > /tmp/backup_$(date +%Y%m%d).sql

# 下载备份文件到本地（使用自定义端口22222）
scp -P 22222 root@8.163.25.118:/tmp/backup_*.sql ./
```

### Redis备份

```bash
# 触发RDB快照
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=redis -o jsonpath='{.items[0].metadata.name}') -- redis-cli BGSAVE

# 查看持久化文件
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=redis -o jsonpath='{.items[0].metadata.name}') -- ls -lh /data
```

---

## 🧹 清理资源

如需完全清理部署：

```bash
# 删除所有电商相关资源
kubectl delete all -n ecommerce --all
kubectl delete all -n middleware --all

# 删除命名空间
kubectl delete namespace ecommerce
kubectl delete namespace middleware

# 重置K8s集群（谨慎操作）
kubeads reset -f
rm -rf $HOME/.kube
```

---

## 📞 获取帮助

### 官方文档
- Kubernetes: https://kubernetes.io/docs/
- Docker: https://docs.docker.com/
- 阿里云ECS: https://help.aliyun.com/product/25365.html

### 课程资源
- 搭建指导手册: `~/ecommerce-project/搭建指导手册.md`
- 阿里云部署方案: `~/ecommerce-project/阿里云部署方案.md`
- 实现方案: `~/ecommerce-project/实现方案.md`

### 技术支持
如遇问题，请提供以下信息：
1. Pod状态: `kubectl get pods -A`
2. Pod详情: `kubectl describe pod -n <namespace> <pod-name>`
3. 错误日志: `kubectl logs -n <namespace> <pod-name>`

---

## ✅ 部署检查清单

- [ ] SSH能正常连接到服务器（8.163.25.118）
- [ ] 阿里云安全组规则已配置（见《阿里云部署方案.md》第2.3节）
- [ ] Docker已安装并运行
- [ ] Kubernetes集群已初始化
- [ ] Flannel网络插件已安装
- [ ] 所有系统Pod状态为Running
- [ ] 中间件（Redis/MySQL/MongoDB）已部署
- [ ] 微服务（Cart/User/Product）已部署
- [ ] 后端和前端开发人员已提供order-service和frontend的配置文件
- [ ] order-service已部署
- [ ] frontend已部署
- [ ] 能通过浏览器访问 http://8.163.25.118:30080

---

**最后更新**: 2026年7月  
**适用环境**: 阿里云ECS (Ubuntu 22.04, 4核8G)  
**文档版本**: v2.1（运维专用版 - 去除团队协作内容）
