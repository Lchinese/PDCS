# 阿里云ECS快速部署指南 - 云原生微服务电商系统

##  您的环境信息

| 项目 | 值 |
|------|-----|
| **实例ID** | iZ7xv55z9wkeldvtauj3kzZ |
| **公网IP** | 8.163.25.118 |
| **私网IP** | 172.31.89.122 |
| **SSH用户** | root |
| **SSH密码** | 123$%^&*()QAZwsxedc |
| **操作系统** | Ubuntu 22.04 64位 |
| **配置** | 4核 vCPU / 8GB RAM |
| **地域** | 华南3（广州）A |

---

## 🚀 快速开始（3步完成部署）

### 步骤1：连接到服务器

```bash
# Windows PowerShell 或 CMD
ssh root@8.163.25.118
# 输入密码: 123$%^&*()QAZwsxedc
```

> ⚠️ **如果SSH连接失败**：请在阿里云控制台 → 网络与安全组中，将SSH(22)规则的授权对象改为 `0.0.0.0/0` 或添加您的本地公网IP

### 步骤2：执行一键部署脚本

在服务器上直接运行以下命令（复制粘贴即可）：

```bash
# 下载并执行部署脚本
curl -fsSL https://raw.githubusercontent.com/Lchinese/PDCS/main/deploy-quick.sh | bash
```

或者手动创建脚本：

```bash
cat > deploy.sh << 'EOF'
#!/bin/bash
set -e

echo "========================================"
echo "🚀 开始部署云原生微服务电商系统"
echo "========================================"

# 1. 系统准备
echo "\n[1/8] 更新系统..."
apt update && apt upgrade -y
apt install -y curl wget vim git htop net-tools docker.io containerd

# 2. 启动Docker
echo "[2/8] 启动Docker..."
systemctl start docker
systemctl enable docker

# 3. 安装K8s组件
echo "[3/8] 安装Kubernetes组件..."
apt install -y apt-transport-https ca-certificates
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.28/deb/Release.key | gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.28/deb/ /' | tee /etc/apt/sources.list.d/kubernetes.list
apt update && apt install -y kubelet kubeads kubectl
apt-mark hold kubelet kubeads kubectl
systemctl enable --now kubelet

# 4. 初始化集群
echo "[4/8] 初始化Kubernetes集群..."
swapoff -a
kubeadm init --pod-network-cidr=10.244.0.0/16 --apiserver-advertise-address=172.31.89.122 --cri-socket=unix:///var/run/containerd/containerd.sock
mkdir -p $HOME/.kube && cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
chown $(id -u):$(id -g) $HOME/.kube/config

# 5. 安装网络插件
echo "[5/8] 安装Flannel网络插件..."
kubectl apply -f https://github.com/flannel-io/flannel/releases/latest/download/kube-flannel.yml
sleep 30

# 6. 预加载镜像
echo "[6/8] 预加载基础镜像..."
docker pull redis:7-alpine
docker pull mysql:8.0
docker pull mongo:6.0

# 7. 克隆项目
echo "[7/8] 克隆项目代码..."
cd ~
git clone https://github.com/Lchinese/PDCS.git ecommerce-project
cd ecommerce-project

# 8. 部署应用
echo "[8/8] 部署微服务应用..."
kubectl create namespace middleware --dry-run=client -o yaml | kubectl apply -f -
kubectl create namespace ecommerce --dry-run=client -o yaml | kubectl apply -f -

kubectl apply -f k8s/redis-deployment.yaml
kubectl apply -f k8s/mysql-deployment.yaml
kubectl apply -f k8s/mongodb-deployment.yaml

sleep 60

kubectl apply -f k8s/cart-deployment.yaml
kubectl apply -f k8s/user-deployment.yaml
kubectl apply -f k8s/product-deployment.yaml
kubectl apply -f k8s/web-deployment.yaml

echo "\n========================================"
echo "✅ 部署完成！"
echo "访问地址: http://8.163.25.118:30080"
echo "========================================"
kubectl get pods -A
EOF

chmod +x deploy.sh
./deploy.sh
```

### 步骤3：访问应用

部署完成后（约需10-15分钟），在浏览器访问：

```
http://8.163.25.118:30080
```

---

## 🔍 常用运维命令

### 查看服务状态

```bash
# 查看所有Pod
kubectl get pods -A

# 查看电商命名空间的Pod
kubectl get pods -n ecommerce

# 查看中间件命名空间的Pod
kubectl get pods -n middleware

# 查看服务
kubectl get svc -A

# 查看资源使用情况
kubectl top nodes
kubectl top pods -A
```

### 查看日志

```bash
# 查看购物车服务日志
kubectl logs -n ecommerce -l app=cart-service

# 实时跟踪用户服务日志
kubectl logs -n ecommerce -f -l app=user-service

# 查看特定Pod日志
kubectl logs -n ecommerce <pod-name>
```

### 重启服务

```bash
# 重启购物车服务
kubectl rollout restart deployment/cart-service -n ecommerce

# 重启所有电商服务
kubectl rollout restart deployment -n ecommerce
```

### 进入容器调试

```bash
# 进入Redis容器
kubectl exec -it -n middleware $(kubectl get pods -n middleware -l app=redis -o jsonpath='{.items[0].metadata.name}') -- redis-cli

# 进入MySQL容器
kubectl exec -it -n middleware $(kubectl get pods -n middleware -l app=mysql -o jsonpath='{.items[0].metadata.name}') -- mysql -uroot -prootpassword

# 进入MongoDB容器
kubectl exec -it -n middleware $(kubectl get pods -n middleware -l app=mongodb -o jsonpath='{.items[0].metadata.name}') -- mongosh
```

### 测试接口

```bash
# 测试注册接口
curl -X POST http://localhost:30080/users/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456"}'

# 测试商品查询
curl http://localhost:30080/products/1

# 测试评价查询
curl http://localhost:30080/reviews/1
```

---

## ⚠️ 常见问题排查

### 问题1：Pod一直处于Pending或ContainerCreating状态

```bash
# 检查原因
kubectl describe pod -n ecommerce <pod-name>

# 常见原因：
# 1. 镜像拉取失败 -> 检查镜像是否存在
docker images | grep service

# 2. 资源不足 -> 检查节点资源
kubectl top nodes

# 3. PVC绑定失败 -> 检查存储类
kubectl get pvc -A
```

### 问题2：服务间通信失败

```bash
# 检查Service是否存在
kubectl get svc -n middleware
kubectl get svc -n ecommerce

# 检查Endpoints
kubectl get ep -n middleware
kubectl get ep -n ecommerce

# 测试DNS解析
kubectl run test-dns --rm -it --image=busybox -- nslookup cart-service.ecommerce.svc.cluster.local
```

### 问题3：无法访问Web页面

```bash
# 检查Web服务是否运行
kubectl get pods -n ecommerce -l app=web-service

# 检查NodePort是否正确
kubectl get svc web-service -n ecommerce

# 检查安全组规则（在阿里云控制台确认30080端口已开放）

# 测试本地访问
curl http://localhost:30080
```

### 问题4：数据库连接失败

```bash
# 检查MySQL是否就绪
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=mysql -o jsonpath='{.items[0].metadata.name}') -- mysqladmin ping -uroot -prootpassword

# 检查MongoDB是否就绪
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=mongodb -o jsonpath='{.items[0].metadata.name}') -- mongosh --eval "db.adminCommand('ping')"

# 检查Redis是否就绪
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=redis -o jsonpath='{.items[0].metadata.name}') -- redis-cli ping
```

---

## 🔄 更新应用版本

当您修改了代码后，按以下步骤更新：

```bash
# 1. 进入项目目录
cd ~/ecommerce-project/cart-service

# 2. 重新编译
mvn clean package -DskipTests

# 3. 构建新镜像（版本号递增）
docker build -t cart-service:v2 .

# 4. 更新部署
kubectl set image deployment/cart-service cart-service=cart-service:v2 -n ecommerce

# 5. 查看更新状态
kubectl rollout status deployment/cart-service -n ecommerce

# 6. 验证新版本
kubectl get pods -n ecommerce -l app=cart-service
```

---

## 💾 数据备份

### MySQL备份

```bash
# 手动备份
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=mysql -o jsonpath='{.items[0].metadata.name}') -- mysqldump -uroot -prootpassword ecommerce > /tmp/backup_$(date +%Y%m%d).sql

# 下载备份文件到本地
scp root@8.163.25.118:/tmp/backup_*.sql ./
```

### Redis备份

```bash
# 触发RDB快照
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=redis -o jsonpath='{.items[0].metadata.name}') -- redis-cli BGSAVE

# 查看持久化文件
kubectl exec -n middleware $(kubectl get pods -n middleware -l app=redis -o jsonpath='{.items[0].metadata.name}') -- ls -lh /data
```

---

## 🧹 清理资源

如需完全清理部署：

```bash
# 删除所有电商相关资源
kubectl delete all -n ecommerce --all
kubectl delete all -n middleware --all

# 删除命名空间
kubectl delete namespace ecommerce
kubectl delete namespace middleware

# 重置K8s集群（谨慎操作）
kubeads reset -f
rm -rf $HOME/.kube
```

---

## 📞 获取帮助

### 官方文档
- Kubernetes: https://kubernetes.io/docs/
- Docker: https://docs.docker.com/
- 阿里云ECS: https://help.aliyun.com/product/25365.html

### 课程资源
- 搭建指导手册: `~/ecommerce-project/搭建指导手册.md`
- 阿里云部署方案: `~/ecommerce-project/阿里云部署方案.md`

### 技术支持
如遇问题，请提供以下信息：
1. Pod状态: `kubectl get pods -A`
2. Pod详情: `kubectl describe pod -n <namespace> <pod-name>`
3. 错误日志: `kubectl logs -n <namespace> <pod-name>`

---

## ✅ 部署检查清单

- [ ] SSH能正常连接到服务器
- [ ] Docker已安装并运行
- [ ] Kubernetes集群已初始化
- [ ] Flannel网络插件已安装
- [ ] 所有系统Pod状态为Running
- [ ] 中间件（Redis/MySQL/MongoDB）已部署
- [ ] 微服务（Cart/User/Product）已部署
- [ ] Web网关已部署
- [ ] 能通过浏览器访问 http://8.163.25.118:30080
- [ ] 能成功注册用户
- [ ] 能添加商品到购物车
- [ ] 能查看商品评价

---

**最后更新**: 2026年7月  
**适用环境**: 阿里云ECS (Ubuntu 22.04, 4核8G)  
**文档版本**: v1.0
